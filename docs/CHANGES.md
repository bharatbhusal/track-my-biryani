# Recent Changes

- Centralized datetime utilities (src/lib/datetime.ts)
- Deterministic export and upload naming (src/lib/naming.ts)
- Quick Add modal with draft autosave and timezone-safe handling
- Glimpse upload: client-side compression and deterministic public_id
- Server: dashboard API accepts `preset`, `from`, `to` query params to scope analytics
- Time range selector and wiring from dashboard view to server
- Theme-aware toast notifications via `ToastProvider`
- Various lint fixes and accessibility improvements

See other docs for API and architecture details.
