# Daily Expenses Tracker

Production-ready expense tracker built with Next.js App Router, TypeScript, TailwindCSS, React Query, Zustand, Mongoose, JWT auth, and Cloudinary signed uploads.

## Quick Start

1. Install dependencies

```bash
npm install
```

2. Configure environment

```bash
cp .env.example .env.local
```

3. Run the app

```bash
npm run dev
```

## Scripts

- `npm run dev` - start development server
- `npm run lint` - run eslint
- `npm run build` - create production build
- `npm run start` - run production server

## Architecture

```text
src/
├── app/
├── components/
├── features/
├── hooks/
├── services/
├── store/
├── lib/
├── models/
├── repositories/
├── controllers/
├── middleware/
├── styles/
├── animations/
└── types/
```
